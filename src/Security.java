public class Security extends User {
    // Constructors
    public Security() {
        super(); // Call superclass constructor
    }

    public Security(String firstName, String lastName, String username, String password) {
        super(firstName, lastName, username, password);
    }
}

